import { Button, View } from 'react-native';
import { useRouter } from 'expo-router';

export default function dailyJournal() {
  const router = useRouter();
  return (
    <View>
        {/* Solo es decorativo, se implementara más adelante */}
    </View>
  );
}